<script defer src="/js/deleteRoute.js"></script>
<script defer src="/js/editRoute.js"></script>
<script defer src="/js/showRoute.js"></script>

{{#if sessionBool}}

<div id="map" style="width: 100%; height: 40%"></div>

<form action="/routes" method="POST">

  <div class="ui equal width center aligned padded grid">

        <div class="content">

          <div class="ui huge header">
            <a href="/routes/show/{{routeByID.id}}"><i class="marker icon"></i>{{routeByID.routeTitle}}</a>
          </div>

          <div class="meta">
            {{routeByID.datetimeStart}}-{{routeByID.datetimeFinish}}
          </div>
          <div class="description">
            {{routeByID.description}}
          </div>

          <div class="hidden-coord" style="visibility: hidden">
            {{routeByID.coordinates}}
          </div>

          

          <div class="ui two buttons">
            <button type="submit" class="ui button delete"><a href="/routes/{{routeByID.id}}">DELETE</a></button>
            <button type="submit" class="ui secondary button change"><a href="/routes/edit/{{routeByID.id}}"
                data-id="{{routeByID.id}}">EDIT</a></button>
          </div>
        
     

    </div>

  </div>
{{!-- 
  <h1>{{routeByID.routeTitle}}</h1>
  <h4>Description: {{routeByID.description}}</h4>
  <h4>Date and time: {{routeByID.datetimeStart}}{{routeByID.datetimeFinish}}</h4>
  <h4>Creator: {{routeByID.creator}}</h4>
  <button type="submit" class="delete"><a href="/routes/{{routeByID.id}}">DELETE</a></button>
  <button type="submit" class="change"><a href="/routes/edit/{{routeByID.id}}"
      data-id="{{partyByID.id}}">EDIT</a></button> --}}
</form>
{{/if}}
